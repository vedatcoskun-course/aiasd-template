# Week 1 — the minimum needed to start.
# Heavier dependencies arrive per week: see requirements/weekNN.txt
#
#   pip install -r requirements.txt

streamlit==1.41.1
python-dotenv==1.0.1
requests==2.32.3
