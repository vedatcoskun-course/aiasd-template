# Week 3 — local models, cloud APIs and embeddings.
# Install on top of the root requirements.txt:
#
#   pip install -r requirements.txt -r week03/requirements.txt
#
# Heads up: sentence-transformers pulls in PyTorch (~2 GB) and the BGE-M3
# model downloads a further ~2.2 GB on first use. Use a good connection.

-r ../requirements.txt

ollama==0.4.5
anthropic==0.42.0
google-generativeai==0.8.3
sentence-transformers==3.3.1
numpy==2.2.1
